
let left_operand = parseInt(prompt("Enter the first number for addition:"));
let right_operand = parseInt(prompt("Enter the second number for addition:"));
let ans = left_operand + right_operand;
console.log(ans);


left_operand = parseInt(prompt("Enter the first number for subtraction:"));
right_operand = parseInt(prompt("Enter the second number for subtraction:"));
ans = left_operand - right_operand;
console.log(ans);


left_operand = parseInt(prompt("Enter the first number for multiplication:"));
right_operand = parseInt(prompt("Enter the second number for multiplication:"));
ans = left_operand * right_operand;
console.log(ans);


left_operand = parseInt(prompt("Enter the first number for division:"));
right_operand = parseInt(prompt("Enter the second number for division:"));
ans = left_operand / right_operand;
console.log(ans);